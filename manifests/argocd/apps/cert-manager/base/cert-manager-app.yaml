apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cert-manager
spec:
  destination:
    namespace: openshift-cert-manager
    server: https://kubernetes.default.svc
  project: cluster-config
  source:
    path: manifests/argocd/apps/cert-manager/base/cert-manager-webhook/base
    repoURL: https://github.com/StinkyBenji/fluentocp-gitops
    targetRevision: main
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: false
      selfHeal: false